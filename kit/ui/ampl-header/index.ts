/**
 * AmplHeader barrel — the component, the default tool registry, and the public types.
 *
 * @version v0.3.0
 */
export { AmplHeader } from "./AmplHeader";
export { DEFAULT_TOOLS } from "./tools";
export type {
  AmplHeaderProps,
  NavItem,
  ToolLink,
  ToolId,
  AccountInfo,
  HeaderSize,
} from "./types";
